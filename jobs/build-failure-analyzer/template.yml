#
# Job template for Build failure analyzer
#
parameters:
  serviceConnection: ""

steps:
  - template: ../../steps/build-failure-analyzer.yml
    parameters:
      serviceConnection: ${{ parameters.serviceConnection }}
