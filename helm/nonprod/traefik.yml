imageTag: latest

# If this doesn't work (i.e. the IP is taken), then comment out, let it get assigned dynamically and register in Consul manually
loadBalancerIP: 10.97.17.51
resources:
  limits:
    cpu: 1000m
    memory: 300Mi
service:
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
rbac:
  enabled: true
dashboard:
  enabled: true
  domain: traefik.service.core-compute-preview.internal
ssl:
  enabled: true